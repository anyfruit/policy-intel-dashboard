# ─── Web 框架 / API ───
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ─── 抓取 ───
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
feedparser>=6.0.11

# ─── 配置 ───
PyYAML>=6.0.1

# ─── 认证 ───
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1            # passlib 与 bcrypt 5.x 不兼容，固定此版本
python-jose[cryptography]>=3.3.0

# ─── 正文提取（可选）───
trafilatura>=1.8.0   # 如安装失败可注释掉

# ─── 定时任务（可选）───
# schedule>=1.2.0
